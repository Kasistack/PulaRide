<?xml version="1.0" encoding="utf-8"?>
<!--
  Auto-backup is disabled at the manifest level (android:allowBackup="false"),
  so this file is defensive. If you later enable backup, ONLY non-secret app
  data may be included. Token / credential / wallet state must stay excluded.
-->
<full-backup-content>
    <!-- Nothing is backed up. Add <include> entries only for non-sensitive data. -->
</full-backup-content>
